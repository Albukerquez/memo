<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label 'Комментарий' %><br>
    <%= f.text_area :content, class: 'form-control' %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
